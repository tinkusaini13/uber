@Library('tinku_cloud_lib') _

pipeline {
    agent any
    
    stages {
        stage('Git Checkout'){
            steps{
            gitCheckout(
                branch: "main",
                url: "https://github.com/tinkusaini13/uber.git"
            )
            }
        }

        stage('Maven Build : maven'){
           steps{
               script{      
                   mvnBuild()
               }
            }
        }
    }
}
